using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LogCheckBoxes : MonoBehaviour
{


    public static bool CheckBoxBpm = false;
    public static bool CheckBoxCalories = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
